# Dev/test dependencies — NOT included in the Docker production image
# Install locally with: pip install -r requirements-dev.txt
-r requirements.txt

# Security auditing
pip-audit==2.7.3

# Test dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
